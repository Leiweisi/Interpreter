#工作單


沈緯郡	Language Designer (語言設計師) +Interpreter Developer(直譯器發開)|Defines syntax and overall structure(定義語法和整體結構)+Implements the execution logic(實施執行邏輯)

黃雁汝	Lexer Developer(詞語分析器開發) +Error Handler & Debugger(錯誤處理)|Implements the tokenizer(實作詞法分析器) +Manages error handling and debugging(管理錯誤和處理)

黃逸萱	Parser Developer(語法分析器開發)+Project Manager |Builds the AST from tokens(從tokens 建立AST)

# Interpreter
lexer.py:第二週的lexer語法(基本詞彙辨識)
AST_paser:第三週的AST基本語法(節點和遞迴)
